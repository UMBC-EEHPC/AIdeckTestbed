[constants]
arch = 'riscv32-unknown-elf'

[host_machine]
system = 'elf'
cpu_family = 'riscv32'
cpu = 'unknown'
endian = 'little'

[binaries]
c = arch + '-gcc'
cpp = arch + '-g++'
strip = arch + '-strip'